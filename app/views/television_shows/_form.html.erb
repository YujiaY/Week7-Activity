<%= form_for(television_show) do |f| %>
  <% if television_show.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(television_show.errors.count, "error") %> prohibited this television_show from being saved:</h2>

      <ul>
      <% television_show.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :details %>
    <%= f.text_area :details %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
